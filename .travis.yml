language: node_js
node_js:
  - "0.10"
branches:
  only:
  - master
before_script:
  - npm install -g grunt-cli
  - git config --global user.email "mateus.freira@gmail.com"
  - git config --global user.name "Mateus Freira"  